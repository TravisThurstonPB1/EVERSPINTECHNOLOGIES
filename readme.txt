Requirement:

Python 3.x (Latest recommanded)
pip install office365
pip install openpyxl
pip install numpy
pip install pandas

Setup cronjob for sharepoint.py as per the schedule time.
For example: https://crontab.guru/

777 Read Write Persmission if required.